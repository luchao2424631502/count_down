{
  "name": "countdown-pwa",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted Countdown PWA (Days Matter style) — React frontend + TS/Node Express backend + SQLite, cloud sync via LWW",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "dev": "pnpm run dev:server & pnpm run dev:web",
    "build": "pnpm -r build",
    "db:init": "pnpm --filter server run db:init"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "packageManager": "pnpm@10.30.3"
}
